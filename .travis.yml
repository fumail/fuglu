language: python
python:
  - "2.6"
  - "2.7"

install:
  - pip install -r fuglu/tests/travis/test-requirements.txt

script: nosetests fuglu/tests/unit/

branches:
  only:
    - master

notifications:
  email: true
  
cache:
  apt: true
